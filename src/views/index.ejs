<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>

    <body>
        <%- include('./partials/nav.ejs') %>

            <h1>Header</h1>
            <br>
            <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Corporis, quis?</p>

            <h2>All blogs</h2>

            <% if(blogs.length>0){ %>
                <% blogs.forEach(blog=> { %>
                    <h3 class="title">
                        <%= blog.title %>
                    </h3>
                    <p class="snippet">
                        <%= blog.snippet %>
                    </p>
                    <%}) %>

                        <% } else { %>
                            <h3>the blog is Empty</h3>
                            <% } %>

                                <%- include('./partials/footer.ejs') %>

    </body>

</html>